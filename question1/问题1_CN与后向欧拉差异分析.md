# 问题一：Crank–Nicolson 改造与差异分析

## 1. 最终采用的计算方案

主模型和物理参数没有改变，只修改并加密了数值计算方法：

> 说明：`题目分析报告.md` 中的 `N=200、Δt=1 s、后向欧拉` 是改造前的历史基线。本次用户明确要求改用 CN，因此最终数值方案以本报告和 `q1_cn_comparison.py` 为准；原建模文档未被代码阶段擅自覆盖。

- 空间仍采用一维圆柱径向有限体积法；中心处为零通量，表面为对流换热/传质边界。
- 温度和水分分别求解；水分扩散系数仍随含水率变化。
- 主结果采用 Crank–Nicolson（CN）方法、800 个径向区间、内部时间步 0.125 s。
- 内部网格间距为 0.0025 cm，计算完成后再提取题目要求的每 0.1 cm、每 1 s 输出。
- 水分非线性计算采用 Picard 逐次迭代，停止误差为 `1e-11 kg/kg`；所有时间步均收敛，最多迭代 4 次。
- 原后向欧拉（BE）方案保留为 200 个径向区间、时间步 1 s 的基线结果。

主提交文件为 `results/result1.xlsx`，内容与 `results/result1_CN.xlsx` 相同；旧方法结果单独保存在 `results/result1_BE.xlsx`。

## 2. 与截图论文结果的对比

新的 CN 结果与用户提供的论文截图已经基本一致。以 1800 s 温度为例：

| 到中心距离/cm | 本次 CN/℃ | 截图/℃ |
|---:|---:|---:|
| 0.0 | 33.5753 | 33.5753 |
| 0.5 | 33.7720 | 33.7720 |
| 1.0 | 34.3642 | 34.3642 |
| 1.5 | 35.3621 | 35.3621 |
| 2.0 | 36.7856 | 36.7856 |

全部 35 个温度点与截图转录值的最大差为 `5.58e-5 ℃`；全部 35 个水分点的最大差为 `6.46e-5 kg/kg`。这个量级小于或接近四位小数显示时的末位舍入影响，因此可以认为两者数值上已经复现。截图之间本身也有个别末位不同，例如 100 s 表面水分出现过 2.2470 与 2.2474 两个版本，所以不能把截图末位当作更高精度真值。

## 3. 旧结果为什么高出 0.00几

旧结果不是“算错”，而是离散误差略大。控制其他设置不变，只把后向欧拉换成 CN，35 个指定点的最大变化为：

| 差异来源 | 温度最大变化/℃ | 水分最大变化/(kg/kg) | 说明 |
|---|---:|---:|---|
| 时间算法：BE → CN（同网格同步长） | 0.001910 | 0.000411 | 温度差异的首要来源 |
| 空间网格：N=200 → N=800（均为 0.25 s） | 0.000018 | 0.000279 | 水分在表面形成很薄的变化层，因而更吃网格 |
| 界面扩散系数：算术 → 调和平均 | 0 | 0.000001 | 影响很小，不是主要原因 |
| 边界插值：PCHIP → 线性 | 0.002320 | 0.000001 | 温度会受输入曲线细节影响，但线性留点验证更好 |

因此：

1. 旧温度与论文相差约 0.001 ℃，主要由后向欧拉产生。后向欧拉会把随时间变化的过程算得稍微“钝”一些；CN 对时间变化的还原更精细。
2. 水分变化集中在靠近表面的薄层，旧网格对这一薄层刻画不足，所以水分差异更容易受网格影响。
3. 调和平均与算术平均在本题中的差别只有约百万分之一，不足以解释截图差异。
4. 输出四舍五入和不同截图版本会造成最后一位不同，但不是旧结果系统性偏高的主要原因。

## 4. 网格和时间步为什么选 800 与 0.125 s

以本次最细结果为参照，CN 收敛比较如下：

| 检查 | 较粗设置 | 温度最大差 | 水分最大差 |
|---|---:|---:|---:|
| 空间网格 | N=100 | 7.73e-5 ℃ | 1.18e-3 kg/kg |
| 空间网格 | N=200 | 1.84e-5 ℃ | 2.79e-4 kg/kg |
| 空间网格 | N=400 | 3.68e-6 ℃ | 5.56e-5 kg/kg |
| 时间步 | 1.0 s | 7.59e-7 ℃ | 1.02e-6 kg/kg |
| 时间步 | 0.5 s | 1.84e-7 ℃ | 2.43e-7 kg/kg |
| 时间步 | 0.25 s | 3.70e-8 ℃ | 4.86e-8 kg/kg |

结果表明，CN 的时间误差已经很小，主要剩余误差来自水分的空间网格。800 个区间、0.125 s 已足以稳定到题目要求的四位小数附近；继续照搬论文的 N=3200、0.005 s 会把计算量放大很多，但对本题四位小数输出的实际收益有限。

## 5. 边界插值选择

比较了 Linear、PCHIP、自然三次样条和 Akima。内部留一验证中，线性插值对温度和水分的 MAE、RMSE 都最低，而且没有区间过冲。因此正式计算继续采用线性插值。PCHIP 虽更光滑，但本题没有证据表明它更准确；它给温度结果带来的最大变化约 0.00232 ℃，仍然很小。

## 6. 数值可靠性

- CN 与 BE 全时空结果均无 NaN/Inf，温度、水分均在合理范围内。
- 温度由中心向表面递增，水分由中心向表面递减，符合“表面先升温、先失水”的物理过程。
- CN 最大线性方程残差为 `6.68e-13`。
- CN 累计热量平衡残差为 `5.82e-11 ℃`，累计水分平衡残差为 `1.54e-14 kg/kg`。
- 常系数圆柱退化问题与 Bessel 级数解析解的最大误差：温度 `3.21e-5 ℃`，水分 `1.26e-4 kg/kg`。

这些检查属于数值验证，说明程序解得稳定、离散正确；由于附件只提供环境输入而没有药材内部实测值，不能把它称为实验验证。

## 7. 可复现入口与关键文件

从项目根目录运行：

```powershell
python q1_solver.py
```

关键结果：

- `results/result1.xlsx`：CN 主提交结果。
- `results/result1_CN.xlsx`：CN 留档。
- `results/result1_BE.xlsx`：后向欧拉留档。
- `results/问题1_差异来源量化.csv`：各差异来源的定量比较。
- `results/问题1_CN收敛性.csv`：网格和时间步收敛结果。
- `results/问题1_论文截图逐点对比.csv`：与截图逐点对比。
- `results/问题1_CN_BE校验摘要.json`：参数、关键数值与验证指标。
