# 问题四工作笔记

## 输入来源

- 题目原文：待核对 A题.pdf。
- 附件 1：烘房温度和水分环境时序。
- 附件 2：药材半径随时间变化。
- 附件 4：题面中的问题四物性公式。
- 问题三：固定半径 Crank–Nicolson + Picard 求解实现与验证结果。

## 已确认模型合同

- 参考坐标为 `xi=r/R(t)`，计算域固定在 `[0,1]`。
- 几何尺度在每个时间步取中点 `R(t_{n+1/2})`。
- 温度和水分扩散项含 `1/R(t)^2`；移动表面 Robin 边界含 `1/R(t)`。
- 不再额外添加收缩对流项。
- 附录 4 扩散系数中的温度使用 K。
- 另跑附录 4 固定半径控制模型，只隔离半径收缩效应。

## 输入核对结果

- 附件 1：241 行数值，0--14400 s、间隔 60 s，三列为时间/温度/水分浓度。
- 附件 2：145 行数值，0--259200 s、间隔 1800 s；半径 2.000 cm 单调降至 1.198 cm，无缺失、无反弹。
- 表 6：每隔 6 h，固定物理位置按 0.5 cm 间隔，并单列“药材表面”。
- result4.xlsx：每隔 60 s，固定物理位置按 0.1 cm 间隔；收缩后处于药材外的位置必须留空，另保留移动表面值。
- 附录 4 四条物性公式与交接文档一致；扩散系数温度为 K。
- Python 依赖审计：NumPy 2.0.2、Pandas 2.3.3、Matplotlib 3.9.4 可用；SciPy 已由问题三实际使用。

## 最小算例

- 命令：`python code/q4_solver.py --smoke`
- 范围：N=20、dt=2 s、真实附件 1/2、0--600 s。
- 结果：退出码 0，300 步全部收敛，无负值/NaN/Inf，半径校验通过，温湿方程残差分别约 6.64e-13、1.33e-15。

## 粗网格时间范围探查

- 收缩模型 `N=60, dt=2 s`：临界时间约 184059.553 s（51.1277 h），事件半径 1.200 cm，在附件2覆盖范围内。
- 固定2 cm控制模型 `N=60, dt=2 s`：120 h时 `Cmax=0.157226`，尚未达标；正式控制计算上限改为180 h。

## 正式计算

- 收缩正式解 `N=200,p=1.75,dt=0.5 s`：183938.126160 s（51.093924 h）。
- 收缩空间细化 `N=400,dt=0.5 s`：183929.082642 s，与正式解差9.043518 s。
- 收缩时间细化 `N=400,dt=0.25 s`：183929.083008 s，与同网格dt=0.5 s差0.000366 s。
- 附录4固定半径控制 `N=200,dt=0.5 s`：467480.339050 s（129.855650 h）。
- 四组事件最大值均位于中心；Picard未收敛步、负值、NaN/Inf和径向单调性破坏均为0。

## 数据剖析与图表契约

- 附件2为145×2连续变量，无缺失，半径范围1.198--2.000 cm；其分布因后期平台而右偏，不把IQR识别出的平台前数据误称为异常值。
- 附件1为241×3连续变量，无缺失；温度与环境水分浓度相关系数0.983，反映同一升温控制过程，不作因果解释。
- 核心结论：动态半径单调收缩并在约1.2 cm平台；收缩模型数值收敛；全域最湿点在中心；在相同附录4物性下，收缩显著缩短烘干时间。
- 证据面板：raw展示半径/环境/收缩增量/物性，process展示网格随半径变化、空间与时间细化、4 h连续性及平均/全域判据，result展示阈值事件、物理坐标剖面和受控对照。
- 后端与导出：Python，中文通用期刊样式；主图6.8×4.2英寸（多面板酌情加高），400 DPI PNG、可编辑文字SVG/PDF、灰度预览。
- 统计口径：均为确定性数值解，不绘制不存在的置信区间；同变量跨图保持同色，并以线型冗余编码。
