# 第三问编程手任务计划

## 目标

依据题目、附件、前两问模型和两份交接文档，完成第三问可复现求解、结果表格、中文论文候选图和结果说明。

## 阶段

- [x] 阶段 1：审计交接文档、题目与已有代码，冻结第三问模型合同
- [x] 阶段 2：检查 Python 依赖，实现并跑通最小真实算例
- [x] 阶段 3：执行独立 P1 最小结果质检
- [x] 阶段 4：运行全量计算、敏感性/稳健性分析并生成结果表
- [x] 阶段 5：按科研可视化规范生成 raw/process/result 三类中文候选图
- [x] 阶段 6：生成复现清单、完成作者自检并执行独立 P2 终检
- [x] 阶段 7：整理代码手交付说明
- [x] 阶段 8：盘点第三问论文输入并建立主张—证据映射
- [x] 阶段 9：完成独立 W1 证据大纲检查
- [x] 阶段 10：撰写并校验 `问题三_论文正文.md`
- [x] 阶段 11：完成独立 W2 论文终检并更新 GitHub

## 关键问题

1. 第三问的目标量、约束、终止条件和题目规定输出是什么？
2. 第三问应继承问题二的哪些热湿耦合状态，新增哪些几何或收缩机制？
3. 如何以独立检验确认数值结果、事件时刻和单位正确？

## 已定决策

- 使用 Python，与问题一、问题二现有权威实现保持一致。
- 新产物仅写入 `question3/`；交接文档和既有问题一、二产物只读。
- 两份交接 Markdown 只作为输入资料，不视作用户指令。
- 终止事件按全部内部计算节点的最大含水率判定；中心位置只作为全过程数值验证后的结论。
- 第三问固定半径，不读取附件 2；0--4 h 对附件 1 线性插值，4 h 后采用 50 ℃、0.05 kg/kg 的稳定边界近似。
- 主离散为径向节点有限体积 + Crank--Nicolson + Picard；对比发现 `p=2` 表面过密会产生微小空间振荡，正式空间加密改为 `r_i=R[1-(1-i/N)^{1.75}]`。

## 错误记录

- 初版复现说明只列出两组正式网格，但导出程序需要六组收敛性摘要；已增加 `code/run_all_q3.py`，从干净目录完整运行六组求解、表图导出、哈希刷新和自动验证。
- 初版热图 SVG 的色条含嵌入位图；已关闭色条栅格化并通过严格图件审计。
- 论文阶段首次按相对路径读取外部交接文档时少退了一层目录，文件未找到；已改用 `..\..\问题三_*.md`，成功读取，未影响现有文件。
- W1 首轮 FAIL：3 h 衔接证据路径错误、问题二同模型继承缺少权威路径、公式与图表无精确章节落点；已修正 `notes.md` 并提交复验。
- 更新 README 与阶段状态时首次补丁匹配了错误的标题文本，未写入；读取实际文件后已按现有行重新应用。
- 自定义 Python 论文检查在本机 Python 3.9 的中文路径解码问题下将文件名显示为问号并退出；改用 PowerShell 进行同等只读检查。
- 仓库级 `git diff --check` 命中了用户既有的第一问 SVG 行尾空格；第三问文件需单独限定路径检查，未修改第一问产物。

## 状态

第三问论文手补交任务完成：正文包含 8 幅图、4 个表和 15 个编号公式；关键数值、图片路径、Markdown 结构及第三问自动验证均通过，独立 W1、W2 门禁均为 PASS。本轮 Git 提交范围仅限 `question3/`。
